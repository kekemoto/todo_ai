{
  "name": "todo-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply todo-ai-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply todo-ai-db --remote",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "@types/better-sqlite3": "^7.6.13",
    "better-sqlite3": "^12.8.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.0",
    "wrangler": "^3.85.0"
  }
}
